* Get
ПАРАМЕТРЫ номер роутера
РЕЗУЛЬТАТ хеш тарифов или undef если не найден
ПРИМЕР Router::Get(1)

* Create
ПАРАМЕТРЫ хэш параметров роутера
РЕЗУЛЬТАТ номер тарифа
ПРИМЕР Router::Create({hostname=>'Базовый',ip=>'10.100.2.10'})

* Delete
ПАРАМЕТРЫ номер роутера
РЕЗУЛЬТАТ true или fault
ПРИМЕР Router::Delete(1)

* Modify
ПАРАМЕТРЫ номер роутера, хэш параметров для изменения
РЕЗУЛЬТАТ true или fault
ПРИМЕР Router::Modify(1,{hostname=>'bridge2'})

* List
ПАРАМЕТРЫ
РЕЗУЛЬТАТ массив отсортированный по дате регистрации
ПРИМЕР my $routers = Router::List()
